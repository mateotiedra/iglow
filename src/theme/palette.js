const Palette = () => {
  const STATE_GREY = '#76768E';
  const BLACK = '#08080C';
  const NIGHT = '#0C0C12';
  const RED = '#DC0028';

  return {
    STATE_GREY,
    BLACK,
    NIGHT,
    RED,
  };
};

export default Palette;
